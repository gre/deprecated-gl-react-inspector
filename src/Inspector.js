const React = require("react");
const {
  Component,
  PropTypes,
} = React;

class Inspector extends Component {

  render () {
    const {} = this.props;
    return (
      <div></div>
    );
  }
}

Inspector.propTypes = {
};

module.exports = Inspector;
